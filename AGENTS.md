# 개발 지침

이 저장소에서 코드를 고칠 때 지키는 규칙.

**이 파일이 정본이다.** `CLAUDE.md` 는 이 파일을 가리키기만 한다 — 두 벌로 두면
한쪽만 고쳐지고, 그때부터 사람마다 다른 규칙을 따르게 된다.

## 이 저장소가 무엇인가

**여러 사내 플랫폼이 공통으로 쓰는 틀이다.** 도메인은 없다. 있는 것은 도메인이
무엇이든 매번 다시 만들게 되는 것들뿐이다:

    로그인 · 가입 승인 · 부서(조직도) · 권한 · 사이드바 · 공지 · 알림 · 감사 · 서버 상태

**여기에 도메인을 넣지 않는다.** 넣는 순간 다음 플랫폼은 그것을 지우면서
시작하고, 지우다 보면 공통이던 것까지 함께 갈라진다. 도메인은 포크한 저장소에
얹는다.

## 도메인을 얹는 세 자리

공통 틀에는 도메인을 모르는 화면이 셋 있고, 그 셋은 **레지스트리**로 채워진다.
`shared` 가 도메인 모델을 import 하면 방향이 거꾸로 서고, 그때 import 순서 하나로
서버가 안 뜬다 — 그래서 **도메인이 등록하고 공통이 부른다.**

| 화면 | 등록 지점 |
| --- | --- |
| 홈의 「남은 일」 | `extensions.register_maintenance` |
| 서버 화면의 「쌓인 것」 | `extensions.register_stats` |
| 부서 삭제 확인 | `extensions.register_workspace_reference` |
| 기계 자격의 쓰기 경로 | `scopes.register_write_scope` |

첨부는 등록이 필요 없다 — `owner_table`·`owner_id` 로 **느슨하게 가리키므로**
도메인이 무엇이든 `AttachmentList` 를 한 줄 끼우면 된다. 대신 그 자료의 소유 부서를
`workspace_slug` 로 함께 준다: 안 주면 전역이 되고, 전역은 시스템 관리자만 만진다.

등록은 **`app/main.py` 의 `_register_extensions()` 한 곳**에서 한다. 조립 지점이
하나여야 "이게 왜 안 뜨지" 를 물을 자리가 생긴다.

**등록하지 않으면 안 뜬다.** 특히 부서 참조가 그렇다 — 안 걸면 부서를 지울 때 그
표는 목록에 안 나타나고, 사람은 아무것도 안 걸린 줄 안다.

## 화면 용어

화면에 쓰는 말은 [docs/용어.md](docs/용어.md) 가 정본이다 — **명사형 이름, 공식 동사**(수정 · 삭제 ·
추가 · 선택 · 클릭 · 업로드 · 다운로드 · 표시 · 게시 · 해제). 「고치다 · 지우다 · 넣다 · 누르다 · 걸다」 같은
구어는 화면 문구에 쓰지 않는다(코드 주석은 예외). 새 문구를 적을 때 표에 없는 개념이면 표에 먼저 더한다.

## 이름과 식별자

절차는 [docs/새-플랫폼-만들기.md](docs/새-플랫폼-만들기.md) 가 정본이다. 여기는
**지켜야 하는 것**만 적는다.

- **설치의 이름은 `.env` 가 정한다**(`APP_SLUG` · `APP_NAME` · `APP_TAGLINE` · `EXTENSIONS`).
  번들 하나로 여러 플랫폼(인스턴스)을 띄우므로 이름은 빌드에 굽지 않는다. 코드는
  `get_settings().app_name` 으로 읽고 화면은 서버가 심는 `<meta name="app-*">` 로 받는다 —
  `branding` 두 파일은 **아무것도 안 준 설치의 기본값**(개발 · 시험)과 `ERROR_PREFIX` 뿐이다.
  코드가 `branding` 에서 이름을 import 하면 모든 설치가 틀의 이름으로 보인다 — 구조 시험이 막는다.
- **인스턴스만의 기능은 확장 모듈**(`app/extensions/<이름>` · `src/extensions/<이름>`)로,
  **시스템 관리자 › 서버 › 「확장 모듈」** 에서 켠다(`.env` 의 `EXTENSIONS` 는 기본값). **코어(`modules` · `shared`)는 확장을 import 하지 않는다** —
  끈 인스턴스에서 코어가 안 뜨거나 없는 메뉴를 부르게 되고, 그것은 확장을 켠 개발 PC 에서는
  안 드러난다. 확장의 표도 `all_models.py` 에 적고 마이그레이션은 늘 전부 돈다.
  **확장은 사이드바에 자기 이름의 그룹을 낸다** — 켜고 끄는 일은 사용자에게 「그 기능이 있다 /
  없다」 이고, 「확장」 이라는 바구니는 개발자의 사정이다.
- **DB 이름·refresh 쿠키 이름·PAT 표식은 따로 적지 않는다.** 셋 다 `APP_SLUG`
  에서 나온다. 따로 적으면 언젠가 하나가 안 바뀌고, **안 바뀐 하나는 전부 조용한
  사고로 나타난다**: 같은 DB 를 보면 오류 없이 남의 `users` 표를 읽고, 같은 쿠키
  이름이면 번갈아 로그아웃되고, 같은 토큰 표식이면 「형식은 맞는데 인증이 안 되는」
  상태가 오타와 구별되지 않는다.
- `APP_SLUG` 는 **소문자·숫자 한 덩어리, 32자 이내**다(`matnexus`·`testscope`).
  이 값에서 나온 DB 이름과 토큰 표식이 길이 제한이 있는 자리로 들어간다. **설치 뒤에는
  바꾸지 않는다.**
- **포트만은 손으로 정하고, 플랫폼마다 10씩 벌린다**(MatNexus 8010, TestScope
  8020, CrossAXTF 8030, 이 틀 8040). 조직 전체에서 겹치지 않게 배정하는 값이라 이름에서 나올 수
  없다. 겹치면 개발 백엔드를 내린 순간 프론트 프록시가 옆 플랫폼의 설치본에 붙고,
  화면은 그 사실을 아무 데도 말하지 않는다. 같은 서버의 인스턴스는 설치 때 `APP_PORT` 로.

`tests/unit/test_branding.py` 가 위를 검사한다.

## 구조

- **모듈 이름은 백엔드와 프론트가 같다.** `app/modules/<name>` <-> `frontend/src/modules/<name>`.
  예외는 `tests/architecture/test_boundaries.py` 에 **사유와 함께** 적는다.
- **모듈끼리 라우터를 직접 부르지 않는다.** 조립 지점은 `app/main.py` 하나다.
  모델은 서로를 참조해도 된다(FK 는 본질적으로 그렇다). 로직 공유는 `shared` 를 거친다.
- **`shared` 는 도메인 라우터를 모른다.** 방향은 shared -> 모듈 한 쪽이다.
  알면 순환이 생기고, 그때 import 순서 하나로 서버가 안 뜬다.
- 권한 판정은 `shared/permissions.py` 한 곳에서 한다. 각 쿼리에 흩뿌리면
  "목록에는 보이는데 상세는 403" 같은 어긋남이 생기고, 그때 어느 쪽이 맞는지
  알 방법이 없다.

## 권한의 두 축

**시스템 역할과 부서 역할은 다른 축이다.** 섞으면 화면이 설명할 수 없게 된다.

    is_system_admin  전사. 계정·부서 자체를 만들고 지운다
    부서 manager     그 부서 안에서만. 멤버와 그 부서 소유 자료를 관리한다

부서가 소유하는 표에는 `owner_workspace_id`(nullable) 한 칸을 둔다. **NULL 은
전역**이고 — 여러 부서가 함께 쓰므로 고치는 것은 시스템 관리자뿐이다. 그러면
`visible_owner_clause` · `resolve_owner_workspace` · `require_owner_edit` 가 그대로 붙는다.

**보이는 것과 고칠 수 있는 것은 다른 축이다.** 조직을 가로지르는 물음에 답하는
플랫폼이라면 `open_owner_clause` 를 쓴다 — 자기 부서 것만 보이면 그 물음에 아무도
답할 수 없고, 그러면 사람은 예전처럼 전화를 돌린다. 쓰기는 그것과 무관하게 여전히
소유 부서의 관리자다.

## 데이터

- 새 ORM 모델을 만들면 **`app/all_models.py` 에 import 를 추가**한다. 빠뜨리면
  autogenerate 가 **기존 표를 지우는** 마이그레이션을 만든다. DB 를 만지는
  스크립트도 `app.all_models` 를 import 한다 — 앱에서는 안 드러나고 그 스크립트를
  돌릴 때만 터진다. `tests/architecture` 가 검사한다.
- 지우지 않는다. 계정은 `deleted_at` 만 채우고, 부서는 `is_active=false` 로 보관한다.
  그것으로 만든 자료가 밖에 남아 있고, 몇 년 뒤에도 그것이 무엇이었는지는 물어질 수 있다.
- 삭제·이관 기능을 만들기 전에 **무엇이 그것을 가리키는지 보여 주는 자리**를 먼저
  둔다(`workspaces` 의 `references`). 누르기 전에 아는 것이 이 저장소의 무늬다.
- **행은 마이그레이션에 넣지 않는다.** 첫 부서·관리자·기준정보는 설치 시드
  (`scripts/seed_install.py`)가 심는다. 마이그레이션에 넣으면 모델로 표를 만드는
  시험이 그 행을 못 받아, 같은 목록을 시험 쪽에 한 벌 더 적게 된다.

## API

- 성공 응답은 리소스 그대로, 오류만 엔벨로프.
- **오류 코드를 손으로 잇지 않는다.** `errors.code("MODULE", 1)` 을 쓴다 — 접두사를
  바꾸는 날 한 자리만 안 바뀌면 그 오류는 로그 검색에서 영영 빠지고, 빠졌다는
  사실은 아무 데도 안 뜬다. 시험이 섞였는지 검사한다.
- **오류 본문을 라우트에서 직접 만들지 않는다.** `AppError` 계열을 raise 한다 —
  응답을 만드는 경로가 곧 로그를 남기는 경로여야 한다.
- **부분 수정에서 "안 보낸 것" 과 "비운 것" 을 구별한다.** 안 구별하면 이름 하나
  바꿀 때마다 다른 설정이 함께 초기화되고, 그 손실은 저장한 사람 눈에 안 보인다.
- 목록 엔드포인트에는 서버가 상한을 강제한다(`shared/pagination.py`).
- 스키마를 바꿨으면 `python scripts/export_openapi.py` 와 `npm run api:types` 를
  함께 돌린다. **프론트 타입을 손으로 적지 않는다** — 손으로 적은 타입은 반드시
  서버와 어긋나고, 어긋난 날 화면은 아무 말도 안 하고 undefined 를 그린다.
- **폴링 경로를 만들면 `shared/access_log.py` 의 `_SKIP` 에 더한다.** 안 더하면
  접근 로그가 그 한 줄로 가득 차서 정작 찾을 것을 못 찾는다.
- **빈 결과와 모르는 값은 이유를 달고 나간다.** 0건을 그냥 0건으로 주면 읽는 쪽은
  (사람이든 AI 든) 「없다」 로 읽고 없는 것을 새로 만든다. 목록의 진단이
  `objects/resolve.py` 에 있다 — 새 거르기를 더하면 `_filter_parts` 에도 더해
  「이 조건만 빼면 몇 건」 이 그 조건을 빠뜨리지 않게 한다.
- **오래 걸리는 일은 요청이 아니라 작업이다.** 파일을 받아 처리하거나 파일을 만들어
  주는 경로는 `modules/jobs` 에 종류를 등록하고(`kinds.register`) 202 로 작업을 돌려준다
  — 요청 안에서 돌리면 1만 행에서 분 단위가 되고 클라이언트가 먼저 끊는다. 처리 함수는
  커밋하지 않고, 진행률은 `work.progress` 로, 계획→적용의 두 단계면 지문을 지킨다
  (`docs/작업-워커-설계.md`). 워커(`python -m app.worker`)가 없는 설치에서는 작업이 영영
  대기다 — `deploy.sh` 가 `<slug>-worker` 를 함께 깔고, 개발은 `run.py` 가 띄운다.
- **이름을 받는 자리는 하나로 안 정해지면 거절한다.** 짐작으로 고른 객체는 사람
  눈에 맞는 값처럼 보여서 아무도 안 고친다. 읽는 쪽의 판정은 `object_resolve`,
  쓰는 쪽의 거절은 `objects/bulk.py` 의 참조 풀이다.

## 프론트

- 스타일은 Tailwind 유틸리티로 컴포넌트에 붙인다. 전역 CSS 클래스를 새로 만들지
  않는다.
- API 절대주소를 코드에 넣지 않는다. 항상 상대경로 `/api`.
- shadcn 프리미티브 위에 도메인 컴포넌트를 쌓고, 화면은 조립만 한다.
- **사이드바(`shared/layout/navigation.ts`)가 화면 목록의 정본이다.** 아직 없는
  화면은 `pending` 으로 두면 라우터가 stub 을 만든다 — 제목·단계·설명을 두 곳에
  적지 않는다. `routes/router.test.tsx` 가 어긋남을 잡는다.
- **역할 판정은 `shared/auth/roles.ts` 한 곳에서** 한다. 갈라지면 어떤 화면은
  단추를 보이고 어떤 화면은 안 보이는 상태가 되고, 그 차이는 아무도 설명할 수 없다.
  이것은 **표시일 뿐 권한이 아니다** — 권한은 서버가 판정한다.
- **상태의 말과 색은 `StatusBadge` 가 정한다.** 화면에서 색을 직접 고르면 같은
  "정지" 가 목록에서는 회색이고 상세에서는 빨강이 된다.
- **그림은 `shared/charts` 가 그린다.** recharts·plotly 를 화면에서 직접 import
  하지 않는다 — 축 색·범례 높이·빈 데이터 처리를 화면마다 다시 정하게 되고, 그때
  같은 부서가 이 그림에서는 파랑이고 저 그림에서는 빨강이 된다. 흔한 넷(막대·
  꺾은선·영역·원)은 `Chart`, 무거운 것(히트맵·박스·사케이·3차원)은 `LazyPlot` 이며
  후자는 **쓰는 화면에서만 받는다.** 색은 ReportArchive 와 같은 여덟이다 — 두
  플랫폼의 그림이 한 보고서에 나란히 실리는 날이 온다.
- **차트를 쓰는 화면은 `lazy()` 로 매단다.** 차트 라이브러리가 자주 여는 화면의
  덩어리에 들어가면, 그림을 한 번도 안 여는 사람까지 매번 그만큼을 받는다.
- **타입·묶음의 아이콘 이름은 `shared/icons.ts` 가 그림으로 바꾼다.** 골라 둔 것만
  정적으로 import 한다(lucide 는 3,600개다). **모르는 이름은 기본으로 떨어진다** —
  다른 설치에서 가져온 정의에 여기 없는 이름이 들어 있을 수 있고, 그때 사이드바가
  통째로 안 뜨면 고칠 화면조차 못 연다.
- **빈 목록은 이유를 말한다**(`EmptyState`). "없습니다" 만 적으면 데이터가 없는
  것인지, 거르기가 좁은 것인지, 권한이 없는 것인지 구별할 수 없다.
- **되돌릴 수 없는 일은 무엇이 사라지는지 적는다**(`ConfirmDialog`). "정말
  삭제하시겠습니까" 만 묻는 창은 아무도 안 읽고 예를 누른다 — 읽을 것이 없어서다.
- **`window.prompt` 를 쓰지 않는다.** 오류를 보여 줄 자리가 없어서, 서버가 거절하면
  그 말이 어디에도 안 뜬다.
- **고를 것이 스물을 넘으면 `<Select>` 를 쓰지 않는다**(`shared/components/SearchablePicker`).
  통째로 펼쳐 놓고 눈으로 찾으라고 하면 그 일은 실패하고, **못 찾은 사람은 없다고
  결론 내리고 새로 만든다** — 그러면 같은 값이 둘로 갈린다. 피커는 두 길을 함께
  낸다: 치는 것(이름의 일부를 알 때)과 훑는 것(무엇이 있는지 모를 때). 이미 고를
  수 없는 줄은 **이유를 배지에 적는다** — 비활성만 시키고 말 안 하면 버그로 읽힌다.
- **목록에는 쪽 넘기기를 붙인다**(`shared/components/Pagination`). 서버가 상한을
  강제하므로, 없으면 50건이 넘는 순간 나머지를 볼 방법이 없다 — 그리고 목록은
  잘렸다는 말을 하지 않으므로 사람은 그것이 전부라고 읽는다.
- **본문은 오류 경계 안에 있다**(`shared/components/ErrorBoundary`, AppShell 이
  두른다). 렌더 중 예외가 나면 React 는 트리를 통째로 언마운트하는데, 경계가 없으면
  **아무것도 없는 흰 화면**이 남고 거기엔 원인도 나갈 길도 안 적힌다. 사이드바까지
  감싸지 않는 이유가 그것이다 — 나갈 길은 살아 있어야 한다.

## 스크립트와 배포

배포는 리눅스 · Apptainer 단일 이미지다([ADR 0004](docs/adr/0004-리눅스-Apptainer-배포.md)).
`deploy/` 는 **산출물이 아니라 정본**이다 — `.gitignore` 로 덮지 않는다.

- 셸 스크립트는 `#!/usr/bin/env bash` + `set -euo pipefail` 로 시작한다.
  **`set -e` 만으로는 부족하다** — 파이프 중간의 실패와 안 정해진 변수가 그냥
  지나가고, 그 스크립트는 아무것도 안 한 채 성공했다고 말한다.
- **실행 비트를 지킨다.** Windows 쪽(`\\wsl.localhost\...`)에서 고치면 모드가
  644 로 떨어지고, 그 스크립트는 **다음 배포에서** `Permission denied` 로 죽는다.
  `tests/architecture/test_deploy.py` 가 shebang·`bash`·실행 비트를 검사한다.
- **줄바꿈은 `.gitattributes` 가 정한다 — 개인 설정에 맡기지 않는다.** Windows 의
  Git 은 `core.autocrlf=true` 가 기본이라 그 기계에서 클론하면 셸 스크립트가 CRLF
  로 나온다. 그 파일이 리눅스에 닿으면 `bad interpreter: /usr/bin/env bash^M` 로
  죽는데, **파일도 있고 실행 비트도 있고 눈으로 보면 멀쩡하다.** 같은 시험이
  CRLF 를 검사한다.
- **밀기 전에 `scripts/before_push.sh` 를 돌린다.** CI 가 보는 것을 **같은 환경에서**
  본다 — 특히 `EXTENSIONS` 를 비우고(CI 에는 `.env` 가 없다) 생성물(`openapi.json`)
  재생성을 **맨 끝에** 한다. 그 두 순서를 사람이 기억해야 했을 때 CI 가 두 번 깨졌다.
  한 번에 4~6분으로, CI 왕복 한 번과 같다.
- **릴리스는 태그에서 나온다**(`.github/workflows/release.yml`). 사람 PC 에서 만들어
  올리지 않는다 — `build_bundle.sh` 는 리눅스·apptainer·npm 을 요구하는데 **저장소를
  받는 기계가 Windows 면 아예 못 만들고**, 갖춘 PC 라도 갖춘 정도가 달라 **같은
  태그에서 다른 번들**이 나온다. 검증(`ci.yml`)을 통과해야 게시된다.
- **번들에 담기는 것은 `build_bundle.sh` 가 정본이다.** `README_OPERATOR.md` 가
  번들 안에서 시키는 명령은 번들 안에 있어야 한다 — 어긋나면 운영자가 **없는
  스크립트를 치고**, 그 자리는 대개 SSH 로 서버에 붙어 있는 자리다.
- **배포 자산에 제품 이름을 박지 않는다.** 번들은 `branding.py` 의 기본값만 `BUILD_INFO` 에
  들고, 실제 이름 · 포트 · 확장은 설치가 `deploy.sh` 에 env 로 준다(`/etc/platform-instances/<slug>.conf`
  에 남는다). 박아 두면 빠뜨린 날 새 플랫폼이 **옆 플랫폼의 DB 를 마이그레이션한다.** 구조
  시험이 이것을 지킨다.
- `.env` 는 **BOM 없이** 저장한다. BOM 이 붙으면 **첫 줄 키만 조용히 무시된다** —
  `APP_ENV` 가 기본값으로 떨어져 운영이 development 로 뜬다.
- **`.env` 를 이미지에 굽지 않는다.** 설치마다 다른 값이다. 구우면 비밀번호를
  바꾸는 데 이미지를 다시 만들어야 하고, 그러면 아무도 안 바꾼다. bind-mount 로 넣는다.
- **이미지 루트는 읽기 전용이다.** 앱이 쓰는 곳은 `/data/filestore` 와 `/data/logs`
  뿐이고, 그 둘은 이미지가 `%environment` 에서 스스로 내보낸다. 새로 파일을 쓰는
  코드는 `settings.filestore_dir` 아래에 둔다 — 이미지 안 절대경로를 잡으면
  `[Errno 30] Read-only file system` 이 나는데, **그 메시지는 무엇을 고쳐야
  하는지 말해 주지 않는다.**
- **`pkill -f` 는 자기 명령줄에도 걸린다.** 셸이 스스로를 죽여 종료 코드 15 로
  끝나고 목표는 살아 있다. `pkill -f '[u]vicorn'` 처럼 패턴을 비껴 쓴다.

## 검증

고치고 나서 이 넷을 돌린다.

**마이그레이션을 만들었으면 그 자리에서 개발 DB 에 올린다**(`alembic upgrade head`).
코드 배포와 DB 반영은 다른 일이다 — 서버는 새 코드를 서빙하는데 마이그레이션은
자동이 아니라서, 새 컬럼을 읽는 API 가 500 을 낸다. **명령 사슬에 끼워 두지 말 것** —
앞 단계가 실패하면 조용히 안 돈다.

```bash
cd backend
.venv/bin/ruff format . ../mcp_server ../pipeline --config pyproject.toml
.venv/bin/ruff check . ../mcp_server ../pipeline --config pyproject.toml
.venv/bin/mypy
.venv/bin/python -m pytest
.venv/bin/python -m alembic check
cd ../frontend && npm run build && npm test && npm run lint
```

**MCP 서버는 환경을 갈라 본다.** `mcp` 를 백엔드 개발 환경에 깔면 `starlette.testclient`
가 쓰는 HTTP 스택이 바뀌어 백엔드 시험의 타입이 흔들린다. `mcp_server/` 는 ReportArchive
와 같은 모양이다 — **한 파일 `server.py`**, 그 폴더 안의 `venv`, `./venv/bin/python server.py`
로 띄우는 streamable-http 서버(백엔드 포트 +2). 패키지가 아니므로 `-m` 으로 부르지 않는다.

```bash
cd mcp_server && python3 -m venv venv && ./venv/bin/pip install -r requirements.txt pytest
cd .. && mcp_server/venv/bin/python -m pytest mcp_server/tests
```

진짜 앱에 붙여 보는 시험은 백엔드 쪽(`tests/api/test_mcp_tools.py`)이 가짜 `mcp` 로
도구 함수만 꺼내 돌린다 — 그래서 위의 `pytest` 에 이미 들어 있다.

`alembic check` 가 여기 있는 이유: **시험은 모델로 표를 만들기 때문에**
마이그레이션이 모델과 어긋난 것을 못 잡는다 — 그 어긋남은 배포하고 나서 500 으로
드러난다. (이 저장소의 첫 마이그레이션도 `created_at` 의 NOT NULL 을 빠뜨렸고,
그것을 잡은 것이 `alembic check` 였다.)

테스트는 개발 `.env` 의 접속 정보에서 `<이름>_test` 를 파생해 쓴다.
**개발 DB 를 건드리는 테스트는 쓰지 않는다.**

**시험 DB 는 스위트 하나를 통째로 함께 쓴다.** 「하나뿐이니 막힐 것」 을 기대하는
시험은 앞선 시험이 만든 행 때문에 우연히만 통과한다 — 필요한 상태를 그 시험이
직접 만든다.

## 문서

- 판단이 갈렸던 결정은 `docs/adr/NNNN-제목.md` 에 남긴다 — 결정·배경·대안·결과.
- 실측으로 드러난 함정은 **근거와 함께** 적는다. "이렇게 하세요" 보다 "이렇게 안
  하면 이런 일이 났다" 가 다음 사람에게 유용하다.
